import React from "react";

export const Select = ({ options = [] }) => (
    <select>
        {/* {options.map((item) => (
            <option value={item.value}>{item.text}</option>
        ))} */}
    </select>
);
